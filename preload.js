// Canbox preload script
// Canbox API is available via window.canbox

console.log('ImageBox preload script loaded');

// You can expose additional APIs here if needed
// contextBridge is not needed as canbox injects APIs directly
